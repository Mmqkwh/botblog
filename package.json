{
  "name": "blogmaster-pro",
  "type": "module",
  "version": "6.0.0",
  "description": "بوت تيليجرام متقدم لإدارة المدونات بالذكاء الاصطناعي - 12 وكيل AI",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "deploy": "npm run build && npx wrangler pages deploy dist",
    "dev:sandbox": "wrangler pages dev dist --ip 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "hono": "^4.12.7"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
